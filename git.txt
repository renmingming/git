first git init;
first git commit;
renmingming；
git config --global user.name "" 修改配置 user.eamil
git init 初始化一个git仓库
git add 文件名 添加文件到git仓库
git commit -m '' 完成提交 -m注释
git status 查看当前仓库状态
git diff 查看修改内容